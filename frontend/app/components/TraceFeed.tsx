"use client";

/**
 * TraceFeed
 *
 * Stub component that will eventually render the live SSE trace for a
 * selected deal.
 * TODO: implement — subscribe via lib/api.ts (streamDeal) and render events.
 */

interface TraceFeedProps {
  // TODO: define props (e.g. dealId)
}

export default function TraceFeed(props: TraceFeedProps) {
  // TODO: implement
  return null;
}
